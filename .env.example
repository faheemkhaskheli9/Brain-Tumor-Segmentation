# scripts/segment.py and src/ read no environment variables or secrets at
# runtime -- all inputs are CLI flags (--image/--mask/--output/...) with
# defaults pointing at the checked-in synthetic sample under
# assets/sample_images/. This file is kept for consistency with the
# portfolio-wide scaffold template; there is currently nothing to copy into
# a real .env.
